import React from 'react';
import classes from './InputProfile.module.css';

const InputProfile = (props) => (
  <input className={classes.InputProfile} {...props} />
);

export default InputProfile;
